-- 菜单 SQL
insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}', '${parentMenuId}', '1', '/${moduleName}/${businessName}', 'C', '0', '${permissionPrefix}:view', '#', 'admin', now(), '', null, '${functionName}菜单');

-- 按钮父菜单ID
-- PostgreSQL 无 MySQL 的会话变量与 LAST_INSERT_ID()，改用 perms 唯一标识回查父菜单ID

-- 按钮 SQL
insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}查询', (select menu_id from sys_menu where perms = '${permissionPrefix}:view'), '1',  '#',  'F', '0', '${permissionPrefix}:list',         '#', 'admin', now(), '', null, '');

insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}新增', (select menu_id from sys_menu where perms = '${permissionPrefix}:view'), '2',  '#',  'F', '0', '${permissionPrefix}:add',          '#', 'admin', now(), '', null, '');

insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}修改', (select menu_id from sys_menu where perms = '${permissionPrefix}:view'), '3',  '#',  'F', '0', '${permissionPrefix}:edit',         '#', 'admin', now(), '', null, '');

insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}删除', (select menu_id from sys_menu where perms = '${permissionPrefix}:view'), '4',  '#',  'F', '0', '${permissionPrefix}:remove',       '#', 'admin', now(), '', null, '');

insert into sys_menu (menu_name, parent_id, order_num, url, menu_type, visible, perms, icon, create_by, create_time, update_by, update_time, remark)
values('${functionName}导出', (select menu_id from sys_menu where perms = '${permissionPrefix}:view'), '5',  '#',  'F', '0', '${permissionPrefix}:export',       '#', 'admin', now(), '', null, '');
